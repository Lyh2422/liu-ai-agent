# .env.production（生产环境：vite build 时生效）
# 微信云托管前端与后端同域名，用相对路径 /api（请求会自动发往当前域名的 /api 前缀）
# VITE_API_BASE_URL=/api

# 【备用】若前后端域名不同（如后端在另一个服务），则写完整域名：
VITE_API_BASE_URL=https://liu-ai-agent-back-190785-4-1326772319.sh.run.tcloudbase.com/api